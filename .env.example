# IP address or hostname of this machine (used for browser-accessible service URLs)
# Set this to the machine's network IP so other devices on the network can reach the services.
# On Linux:   ip -4 addr show | grep -oP '(?<=inet\s)\d+(\.\d+){3}' | grep -v 127.0.0.1 | head -1
# On Windows: (Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.InterfaceAlias -notlike '*Loopback*' }).IPAddress
HOST_IP=localhost
